<div class="container px-5 px-sm-0">
  <div class="row">
    <%= render 'public/shared/sidebar', customer: current_customer %>

    <div class="col-md-8 offset-md-1">
    <h2>みんなの投稿</h2>
      <% @pictures.each do |picture| %>
        <div class="card" style="width: 100%; margin-bottom: 10px;">

          <!--イラスト-->
          <div style="border-bottom: 1px solid rgba(0, 0, 0, 0.125);">
            <div id="theTarget">
            <% picture.post_images.each do | image | %>
              <%= image_tag image.variant(resize_to_limit: [100, 100]).processed %>
            <% end %>
            </div>
          </div>

          <div>
            <!--投稿者のアイコンと名前-->
            <div style="display: inline-block;">
            <%= image_tag picture.customer.profile_image.variant(resize_to_limit: [50, 50]).processed, class:"mt-3 rounded-circle bg-primary" %>
            👤<%= link_to picture.customer.name, public_customer_path(picture.customer.id) %>
            </div>

            <!--文章-->
            <div style="display: inline-block;"><%= picture.explain %></div>

            <!--タグ-->
            <div style="display: inline-block;">
              <p>
              <% picture.tags.each do |tag| %>
                <% if tag.cloud == 0 #手動 %>
                  #<%= tag.name %>
                <% else #Cloud API %>
                <span style="color: white; background-color: lightblue; margin: 3px;">
                <%= tag.name %>
                </span>
                <% end %>
              <% end %>
              </p>
            </div>

            <div style="border-top: 1px solid rgba(0, 0, 0, 0.125);">

            <!--いいね-->
            <div style="display: inline-block;">
              <% if picture.liked_by?(current_customer) %>
                <%= link_to public_like_path(current_customer.likes.find_by(picture_id: picture.id)), method: :delete do %>
                    ♥<%= picture.likes.count %>
                <% end %>
              <% else %>
                <%= link_to public_likes_path(picture_id: picture.id), method: :post do %>
                  ♡<%= picture.likes.count %>
                <% end %>
              <% end %>
            </div>

            <!--ブックマーク-->
            <div style="display: inline-block;">
              <% if picture.bookmarked_by?(current_customer) %>
                <%= link_to public_bookmark_path(current_customer.bookmarks.find_by(picture_id: picture.id)), method: :delete do %>
                  📖外す
                <% end %>
              <% else %>
                <%= link_to public_bookmarks_path(picture_id: picture.id), method: :post do %>
                  📖登録
                <% end %>
              <% end %>
            </div>

            <div style="display: inline-block;">💬数：<%= picture.comments.count %></div>

            <div style="display: inline-block;"><%= link_to '詳細', picture, class:"btn btn-primary" %></div>

            <div style="display: inline-block;">
              <% if current_customer && picture.customer == current_customer || current_admin %>
              <p><%= link_to '削除', picture, method: :delete, class:"btn btn-danger" %></p>
              <% end %>
            </div>
            </div>
          </div>
          
          <!--検索結果0件-->
          <% if @pictures.blank? %>
            <div>結果0件</div>
          <% end %>

      </div>
      <% end %>
    </div>
  </div>
</div>
