<%= form_for(current_customer.active_relationships.build, url:public_customer_relationships_path(current_customer) )do |f| %>
  <div><%= hidden_field_tag :following_id, @customer.id %></div>
  <%= f.submit "Follow", class: "btn btn-primary" %>
<% end %>